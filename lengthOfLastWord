class Solution {
    public int lengthOfLastWord(String s) {
        String temp = s.trim(); 
        if (s.length()==0){
            return 0;
        }
        temp = temp.substring(temp.lastIndexOf(" ") + 1); 
        return temp.length();
        
}
}
